{{template "base/head" .}}
<div role="main"  class="page-content home">
	<div class="tw-mb-8 tw-px-8">
		<div class="center">
			<img class="logo" width="220" height="220" src="{{AssetUrlPrefix}}/img/logo.svg" alt="LOGO">
			<div class="hero">
				<h1 class="ui icon header title">
					{{AppName}}
				</h1>
				<h2>Version control for Corgis (and maybe humans)</h2>
			</div>
		</div>
	</div>
</div>
{{template "base/footer" .}}
